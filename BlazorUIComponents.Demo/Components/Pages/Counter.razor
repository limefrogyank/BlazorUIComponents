@page "/counter"

@inherits NavPageBase<CounterViewModel>


<h1>Counter</h1>
@if (Vm != null)
{
<p>Current count: @Vm.Count</p>
<ReactiveButton Classes="ui button" Command=@Vm.IncrementCountCommand>Click me</ReactiveButton>
}